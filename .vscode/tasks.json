{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build and run control server",
            "type": "shell",
            "command": "go build -o control && ./control",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}